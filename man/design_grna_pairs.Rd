% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/design_grna_pairs.R
\name{design_grna_pairs}
\alias{design_grna_pairs}
\title{Design gRNA pairs for exon deletions}
\usage{
design_grna_pairs(
  gene_id,
  species,
  id_type = c("symbol", "ensembl_gene_id"),
  nuclease,
  filter_domains = NULL,
  transcript_id = NULL,
  genome,
  return_all = FALSE
)
}
\arguments{
\item{gene_id}{Character. Gene identifier: either a gene symbol
(via external_gene_name) or an Ensembl Gene ID (ENSG...).}

\item{species}{Character. Ensembl short code (e.g., "hsapiens").}

\item{id_type}{Character. One of "symbol" (default) or "ensembl_gene_id".}

\item{nuclease}{Character. Must be specified explicitly.
Currently supported: "Cas9", "Cas12a".}

\item{filter_domains}{Character vector. Optional list of InterPro IDs or descriptions
to focus designs onto (e.g., c("IPR008967", "DNA-binding")).}

\item{transcript_id}{Character. Optional transcript ID to override canonical
transcript choice. Warns if not canonical.}

\item{genome}{BSgenome. Required BSgenome object for target species.}

\item{return_all}{Logical (default FALSE). If TRUE, will retrieve ALL exon pairs
(both phase compatible and incompatible). This is potentially huge, so interactive confirmation is required.}
}
\value{
A data.frame of candidate gRNA pairs with columns:
\itemize{
\item transcript_id
\item exon_5p / exon_3p
\item gRNA_5p / pam_5p
\item gRNA_3p / pam_3p
\item compatible (exon phase check TRUE/FALSE)
\item frameshift (TRUE if not divisible by 3)
\item ptc_flag (TRUE if frameshift causes PTC in non-terminal exon)
\item terminal_exon_case (TRUE if frameshift causes PTC in terminal exon)
}
}
\description{
Integrates exon structure, Cas effector targeting rules, exon phase compatibility,
protein domain annotations, and PTC predictions to propose candidate
gRNA pairs that could induce an in-frame (or allowable frame-shifting) exon deletion.
}
\examples{
\dontrun{
library(BSgenome.Hsapiens.UCSC.hg38)
results <- design_grna_pairs("TP53", "hsapiens",
                             nuclease="Cas9",
                             genome=BSgenome.Hsapiens.UCSC.hg38)
head(results)
}
}

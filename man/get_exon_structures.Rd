% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_exon_structures.R
\name{get_exon_structures}
\alias{get_exon_structures}
\title{Get exon structures for a specified transcript}
\usage{
get_exon_structures(
  transcript_id,
  species,
  output = c("data.frame", "GRanges")
)
}
\arguments{
\item{transcript_id}{Character. Ensembl transcript ID.}

\item{species}{Character. Species short name (e.g., "hsapiens", "drerio", "mmusculus").}

\item{output}{Character. "data.frame" (default when this function is run in isolation) or "GRanges". Use the GRanges output for Cas effector PAM scanning.}
}
\value{
A data.frame or GRanges with:
\itemize{
\item ensembl_exon_id
\item chromosome_name
\item exon_chrom_start / exon_chrom_end
\item strand ("+","-","*")
\item rank
\item start_phase / end_phase
\item cds_start / cds_end (genomic positions of CDS within exon)
\item transcript_cds_length (total CDS length, repeated per exon)
\item exon_cds_length (calculated per exon, NA if non-coding, e.g. UTR)
}
}
\description{
Retrieves exon coordinates, reading frame phases, transcript CDS length,
and exon-specific coding lengths for each exon of a transcript.
}
\examples{
\dontrun{
tx_info <- get_gene_info("TP53", "hsapiens")
canonical_tx <- tx_info$canonical$ensembl_transcript_id[1]
exons_df <- get_exon_structures(canonical_tx, "hsapiens", output="data.frame")
exons_gr <- get_exon_structures(canonical_tx, "hsapiens", output="GRanges")
}
}
